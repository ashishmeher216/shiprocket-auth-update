{
  "name": "shiprocket-auth",
  "version": "1.0.0",
  "description": "This service updates the shiprocket auth token in the elastic beanstalk configuration",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ashish Meher",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1549.0"
  }
}
